<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="DungeonCrawler.NewItemPage" Title="New Item">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Save" Clicked="Save_Clicked" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
        <ScrollView>
		<StackLayout Spacing="20" Padding="15">
			<Label Text="Text" FontSize="Medium" />
			<Entry Text="{Binding Item.Text}" FontSize="Small" />
            
			<Label Text="Description" FontSize="Medium" />
            <Entry Text="{Binding Item.Description}" FontSize="Small" />

            <Label Text="Defense" FontSize="Medium" />
            <Entry Text="{Binding Item.defense}" FontSize="Small" />

            <Label Text="Speed" FontSize="Medium" />
            <Entry Text="{Binding Item.speed}" FontSize="Small" />

            <Label Text="Attack" FontSize="Medium" />
            <Entry Text="{Binding Item.attack}" FontSize="Small" />

            <Label Text="Range" FontSize="Medium" />
            <Entry Text="{Binding Item.range}" FontSize="Small" />
           

            
		</StackLayout>
   </ScrollView>

	</ContentPage.Content>
</ContentPage>
